[project]
name = "llm-agent-ccfa-daily"
version = "0.1.0"
description = "Daily CCF-A LLM/agent paper report workflow for Codex."
requires-python = ">=3.10"
dependencies = [
  "pydantic>=2.0",
  "PyYAML>=6.0",
  "requests>=2.31",
  "reportlab>=4.0"
]

[project.scripts]
llm-agent-ccfa-daily = "llm_agent_ccfa_daily.cli:main"
